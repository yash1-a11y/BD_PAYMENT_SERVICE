# Package System Integration

See `docs/PHASE_1.md` (Package Lookup section) for the curl example and expected
response shape to test against the upstream Package System API manually before
wiring the HTTP client here.
